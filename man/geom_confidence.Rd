% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_confidence.R
\name{geom_confidence}
\alias{geom_confidence}
\title{Confidence Interval Layers for ggplot2}
\usage{
geom_confidence(lm, trans = identity, resolution = 500)
}
\arguments{
\item{lm}{A fitted linear model object (\code{lm()}), with a single predictor.}

\item{trans}{A function to transform the predicted values and intervals (e.g., \code{exp} for log-transformed models). Default is \code{identity} (no transformation).}

\item{resolution}{An integer specifying the number of points at which to evaluate the model. Default is 500.}
}
\value{
A list of ggplot2 layers: a confidence ribbon and a fitted line.
}
\description{
Creates a confidence ribbon and a line of best fit based on a linear model.
This function is similar to using \code{geom_smooth(method = "lm")} but allows for a
transformation of the predicted values (and their confidence intervals) via a supplied function.
}
\examples{
\dontrun{
  library(ggplot2)
  
  # Example dataset
  df <- data.frame(x = 1:10, y = exp(1:10 / 2) + rnorm(10))

  # Fit a model on the log scale
  fit <- lm(log(y) ~ x, data = df)

  # Plot original data with points, and add a confidence ribbon and line
  ggplot(df, aes(x, y)) +
    geom_point() +
    geom_confidence(fit, trans = exp)
}
}
